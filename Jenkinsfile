pipeline {
    agent any

    stages {
        stage('Training Model') {
            steps {
                echo 'Training model...'
            }
        }

        stage('Testing Model') {
            steps {
                echo 'Testing model...'
            }
        }

        stage('Predicting') {
            steps {
                echo 'Running predictions...'
            }
        }

        stage('Finished') {
            steps {
                echo 'Pipeline completed.'
            }
        }
    }
}
